name: CI

on:
  pull_request:
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'test.configs/**'
      - '.vscode/'
      - 'appcast.xml'
  push:
    branches:
      - main
      - dev
      - android-fix-action-bug
      - new-design-v2
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.vscode/'
      - 'appcast.xml'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  run:
    # Важно: файл build.yml должен лежать именно по этому пути
    uses: ./.github/workflows/build.yml
    secrets: inherit
    permissions:
      contents: write
      actions: write
      pull-requests: write
      repository-projects: write
    if: "${{!contains(github.event.head_commit.message, 'release: version')}}"
    with:
      upload-artifact: ${{ github.event_name == 'push' }}
